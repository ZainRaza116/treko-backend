meta {
  name: Create Department
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/departments/
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{admin_token}}
}

body:json {
  {
      "name": "Devs",
      "description": "Team of SEs",
      "organization": "{{organization_id}}",
      "is_active": true
  }
}
